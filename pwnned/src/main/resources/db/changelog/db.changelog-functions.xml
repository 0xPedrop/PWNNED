<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="create-function-calculate-xp" author="pwnned">
        <sql splitStatements="false">
            CREATE OR REPLACE FUNCTION calculate_user_xp(u_id UUID)
            RETURNS INTEGER AS $$
            BEGIN
                -- Lógica: Cada certificado vale 100 pontos de XP
            RETURN (SELECT COALESCE(COUNT(*), 0) * 100
                    FROM certificates
                    WHERE user_id = u_id);
            END;
            $$ LANGUAGE plpgsql;
        </sql>
        <rollback>
            DROP FUNCTION IF EXISTS calculate_user_xp(UUID);
        </rollback>
    </changeSet>
</databaseChangeLog>